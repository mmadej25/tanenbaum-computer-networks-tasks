set(SOURCES generator.cpp )
add_executable(generator ${SOURCES} )

set(SOURCES weryfikator.cpp )
add_executable(weryfikator ${SOURCES} )

set(SOURCES zmieniacz.cpp )
add_executable(zmieniacz ${SOURCES} )
